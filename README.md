# STARLINX / Starlab Agent

Protoapp FastAPI listo para Render (plan gratis).
Rutas útiles: `/`, `/acceso`, `/panel`, `/registros`, `/ping`, `/health`.

Variables:
- ADMIN_KEY (default: starlab123) — cámbiala en Render
- SECRET_KEY — cámbiala en Render (valor largo aleatorio)
